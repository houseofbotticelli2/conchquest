# Conchquest TODO

The single shared task list for this project -- not mirrored anywhere else.
Task numbers are stable references, not priority order.

## Pending

- [ ] #44 Build social feed (PRD MVP item)
- [ ] #46 Build premium subscriptions via RevenueCat (PRD MVP item)
- [ ] #64 Harden Railway build: keep secrets out of Nixpacks build stage
- [ ] #71 Add social login (Google, Apple, Facebook) via Supabase Auth -- Sign in with Apple is mandatory once any other social login is offered and the app is on the App Store (guideline 4.8), not optional; needs provider setup in Apple Developer (Services ID), Google Cloud Console (OAuth client), and Facebook for Developers, wired into Supabase Auth's dashboard, plus expo-apple-authentication + an OAuth flow (expo-auth-session or similar) on the mobile side
- [ ] #75 Draft Privacy Policy, Terms of Service, and Community Guidelines -- required before App Store/Play Store submission (privacy policy is both legally required under GDPR/CCPA and mandated by both stores; ToS needed for account terms + subscription disclosure once #46 ships; Community Guidelines needed to satisfy Apple guideline 1.2's UGC requirement -- a report/block mechanism, tied to #47's admin console). All three now have real drafted content live on `web/` (Privacy/Terms via #89, Community Guidelines drafted separately, also linked from Profile > Settings in the mobile app). Remaining before submission: lawyer review of all three (not just Claude Code), Apple's App Privacy "nutrition label", Google Play's Data Safety section, and a children's-privacy (COPPA) statement.
- [ ] #77 Explore Gemini API image understanding for shell species detection -- feed a find's photo through Gemini's vision capability to suggest/auto-fill the species instead of the user picking manually from the Library; needs a spike to check real-world accuracy against the actual shell_species catalog before committing to it as a real feature
- [ ] #78 Consider bringing the community Leaderboard to the mobile app -- prototyped for fun in the admin console mockup first (ranked by total shells logged, with rare-find and species-diversity as secondary stats); if it's added to the app, needs a real ranking query (or a materialized/cached leaderboard table, since ranking all users live on every request doesn't scale) and a decision on whether it's opt-in/opt-out per user
- [ ] #93 Password reset page (`web/reset-password`) doesn't render on Chrome for iOS -- confirmed working correctly in real Safari on iPhone, in every desktop/Chromium browser tested, and via disposable-account reproduction; only fails specifically in Chrome for iOS. Chrome for iOS has no remote-debugging capability at all (Safari's Web Inspector only attaches to actual Safari; unlike Android Chrome, there's no `chrome://inspect` equivalent), so there's no way to see console output or root-cause it directly. Given it works in the standards-reference browser (Safari) and only affects someone who's both set Chrome as their default browser on iOS *and* taps the email link normally (rather than long-pressing to open in Safari), treating this as a known, low-priority limitation rather than something to keep investigating blind. Revisit if it turns out to affect more than a rare edge case in practice.
- [ ] #83 `web/`'s custom domain is live (`www.conchquest.app`, apex `conchquest.app` forwards to it) -- home, privacy, terms, about, shop, and community-guidelines pages now have real content (see #88/#89, community-guidelines drafted separately, tracked under #75); `download` is still a placeholder.
- [ ] #86 `admin.conchquest.app` is publicly reachable (deployed and DNS-live), protected only by the cookie-session login + admin-role check -- decided to gate it with **Cloudflare Access** (not Tailscale/true VPN): an auth challenge in front of the domain, no VPN client needed on devices. Not yet scoped in detail; when picked back up, the concrete steps are: (1) move `conchquest.app`'s DNS management to Cloudflare (recreate the existing GoDaddy records there first, then cut over nameservers at GoDaddy from `ns43/ns44.domaincontrol.com` to Cloudflare's -- this affects the whole domain's DNS, not just `admin`), (2) create a Cloudflare Access application scoped to `admin.conchquest.app` with an allowed identity list (email OTP or Google/GitHub SSO), (3) verify the existing cookie-session auth still works underneath as a second layer.

## Completed

- [x] #1 Scaffold package.json, tsconfig, env config
- [x] #2 Write PostGIS migrations
- [x] #3 Build environmental aggregation service
- [x] #4 Build deterministic scoring engine /api/score
- [x] #5 Supabase JWT auth middleware
- [x] #6 Build /api/finds endpoint
- [x] #7 Wire up app.ts/server.ts and verify build
- [x] #8 Scaffold Expo/TypeScript project in mobile/
- [x] #9 Build theme system with both palettes
- [x] #10 Port design-system primitives to RN
- [x] #11 Set up navigation skeleton
- [x] #12 Build all 14 screens with prototype sample data
- [x] #13 Add graceful degradation for NOAA tide API flakiness
- [x] #14 Add app_config table + fuzz radius seed values
- [x] #15 Build config service with short-TTL cache
- [x] #16 Build deterministic location fuzzing utility
- [x] #17 Build GET /api/finds/nearby community endpoint
- [x] #18 Convert nearby-finds radius/distance from meters to feet
- [x] #19 Build real interactive map (ShellingMap + react-native-maps)
- [x] #20 Set up Supabase client with session persistence
- [x] #21 Build AuthProvider/useAuth context
- [x] #22 Wire Signup screen to real Supabase auth + login toggle
- [x] #23 Gate navigation on real auth state + add sign out
- [x] #24 Capture display_name from signup on backend
- [x] #25 Build mobile API client with auth token attachment
- [x] #26 Wire Score screen to real /api/score
- [x] #27 Wire Detail screen to real factor breakdown
- [x] #28 Add rarity field to nearby-finds API response
- [x] #29 Add finds API functions to mobile client
- [x] #30 Wire Log screen to real POST /api/finds
- [x] #31 Wire Profile recent finds to GET /api/finds
- [x] #32 Wire Map recent finds nearby to GET /api/finds/nearby
- [x] #33 Use real safe-area insets for tab bar bottom padding
- [x] #34 Seed shell_species catalog with real species data
- [x] #35 Build GET /api/species and /api/species/:id routes
- [x] #36 Add alert_threshold/is_home columns to saved_locations + CRUD routes
- [x] #37 Wire Library screen to GET /api/species
- [x] #38 Wire Species detail screen to GET /api/species/:id
- [x] #39 Wire Saved beaches screen to real saved-locations API
- [x] #40 Add swipe-to-dismiss gesture to SlideUpSheet
- [x] #41 Set up EAS dev client for on-device testing
- [x] #42 Add home-beach checkbox to Saved beaches add form
- [x] #45 Build push notifications (PRD MVP item)
- [x] #48 Wire up photo upload for finds (Cloudflare R2)
- [x] #49 Add /api/config endpoint for recent-finds/beaches limits
- [x] #50 Add PATCH /api/finds/:id endpoint
- [x] #51 Make GET /api/finds/:id support community (non-owner) view
- [x] #52 Wire mobile api.ts for config, getFind, updateFind
- [x] #53 Add Recent Beaches section to Profile (read-only)
- [x] #54 Move Saved beaches management behind the gear/Settings sheet
- [x] #55 Remove wind/conditionSummary line from beach cards
- [x] #56 Wire FindDetail screen to real data (own + community finds)
- [x] #57 Build Edit a Find flow from My Shells
- [x] #59 Make Profile editable (display name, shelling-since year)
- [x] #62 Build multi-day forecast (not just today's snapshot)
- [x] #63 Build push notifications for beach alert thresholds
- [x] #66 Fix Profile avatar "change photo" not working on iOS
- [x] #67 Generate Shelling Strategy card via GPT-4o-mini (OpenAI) -- new POST /api/score/strategy endpoint, cached in its own dedicated shelling_strategy_cache table (keyed by location + day offset, ~24h TTL -- switched from piggybacking on conditions_cache after discovering the Score screen's multi-day forecast never populates that table), falls back to the concatenated factor explanation on error/timeout; verified live end-to-end
- [x] #69 Add caching to the multi-day forecast (/api/score/multi-day) -- new multi_day_forecast_cache table (whole 5-day array per location bucket, reuses conditionsCacheTtlMinutes); verified locally that a repeat request hits the cache instead of re-fetching from NOAA/OpenWeather/NDBC
- [x] #70 Day-strip label: use weekday for all future days, not "Tmrw"; also made beach city read-only in add/edit flows since it's derived from lat/lon (not independently changeable)
- [x] #65 Add Google Maps API key for Android map rendering -- restricted by package name + SHA-1 fingerprint; key must be passed via the react-native-maps plugin's `androidGoogleMapsApiKey` option in app.json's plugins array, NOT the generic `android.config.googleMaps.apiKey` field (that field is only a fallback for packages with no config plugin of their own -- react-native-maps ships its own, which silently strips any existing manifest meta-data when its own option is unset); verified by pulling the built APK and inspecting AndroidManifest.xml directly
- [x] #72 Limit Recent Beaches query to what Profile actually displays -- GET /api/saved-locations now accepts ?limit, avoiding a live conditions/score computation for every saved beach when only a few are ever shown; also joined Profile's remaining sequential requests into its existing Promise.all batch
- [x] #73 Fix Shelling Strategy always saying "tomorrow" regardless of which day was selected -- the dayLabel field was already passed correctly, but neither the DB-seeded app_config system prompt nor the code fallback told the model to anchor its wording to it; migration updates the seeded prompt (which overrides the code fallback) and clears the existing shelling_strategy_cache so stale wrong-wording text isn't served out its remaining TTL
- [x] #74 Add draggable-pin location picker to add/edit beach flows -- ShellingMap gained a draggable center marker (onCenterMarkerDragEnd) and a recenter-to-pin button; add flow starts the pin at current device location, edit flow starts it at the beach's saved location, both re-run reverse geocode on drop to refresh the read-only city field; PATCH /api/saved-locations/:id extended to accept lat/lon/city; web shows an explanatory fallback message since react-native-maps has no web implementation
- [x] #76 Move beach alert cooldown to app_config + add weekly cache cleanup cron -- beach_alert_cooldown_hours (was a hardcoded constant) now matches the existing beach_alert_lead_time_hours pattern; new weekly cron (Sunday 3 AM) removes already-expired rows from conditions_cache/shelling_strategy_cache/multi_day_forecast_cache (never touches live entries -- reads already skip expired rows via expires_at > now(), this just stops them accumulating forever), recording each run's counts in a new cache_cleanup_runs table exposed via GET /api/cache-cleanup-runs; verified locally against the real shared DB
- [x] #47 Build admin console (PRD MVP item) -- new standalone admin/ Vite+React+TS app (Supabase Auth + users.role/requireAdmin gating), covering Dashboard, Members (with per-member finds/beaches detail view + cascading delete incl. Supabase Auth account + R2 photos), Species Library, Prompt Testing (live GPT-4o-mini prompt/temperature/max_tokens editing against canned scenarios), System Config, Service Health (NOAA/NDBC failure chart + failing-stations + cache-cleanup-run panels), Audit Log, and Leaderboard; every mutation is recorded to a new admin_audit_log table. Content Moderation screen deliberately deferred -- there's nothing to moderate until the social feed (#44) exists, and should also add OpenAI usage/spend monitoring for #67 once needed
- [x] #80 Add `SUPABASE_SERVICE_ROLE_KEY` to the deployed API's environment -- confirmed working: deleting a member through the admin console (pointed at the deployed API) successfully removed the Supabase Auth account, not just the mirrored `users` row
- [x] #81 Move admin console auth to httpOnly cookies (BFF layer), off client-side JWTs -- new `/api/admin/session/{login,refresh,logout}` routes call Supabase's grant endpoints server-side and hand the browser only httpOnly cookies (`document.cookie` confirmed empty after login); `requireAuth` accepts the cookie as a fallback to the existing Bearer header so the mobile app's flow is unchanged; CORS moved from a wildcard to an explicit `CORS_ALLOWED_ORIGINS` allowlist since credentialed requests can't use a wildcard origin; cookie `Secure`/`SameSite` keyed off `RAILWAY_ENVIRONMENT` being present rather than `NODE_ENV`, since this project's Railway environment is named "dev" not "production" and `NODE_ENV` isn't set there at all; `@supabase/supabase-js` removed from admin/ entirely, now unused. Verified live end-to-end against the deployed API
- [x] #84 Scaffold `web/` (new Astro + React project) for the public marketing/download/legal site, and build the actual password-reset page on it -- part of #60/#83. `/reset-password` uses a React island (`ResetPasswordForm`) that detects the recovery session via `detectSessionInUrl` and calls `supabase.auth.updateUser()`; home/download/privacy/terms/community-guidelines are placeholders for now. Verified end-to-end using disposable throwaway Supabase accounts (created and deleted via the Admin API, never touching real accounts): generated a real recovery link, confirmed the page detects the session and updates the password, and confirmed via a direct login call that the new password actually works. Also found and fixed a real bug along the way -- Astro's scoped `<style>` blocks don't apply to a React island's rendered markup, needs `is:global`.
- [x] #60 Finish the password reset flow -- Supabase's Auth > URL Configuration was updated (Site URL -> `https://www.conchquest.app`, Redirect URLs allowlist includes `https://www.conchquest.app/reset-password`), fixing the root cause (Supabase was silently falling back to a dead default Site URL for any redirect not on that allowlist). Verified with a fresh recovery link end-to-end on the real domain: the `/verify` redirect and the page itself both confirmed working via direct HTTP checks. Mobile's "Forgot password?" -> web's `/reset-password` -> real password change is now fully live, not just locally. Remaining/separate: user is updating Supabase's email template branding directly in the dashboard (not code).
- [x] #85 Attach custom domains on Railway -- `conchquest.app` (GoDaddy) is split across services in one project: `www.conchquest.app` -> `conchquest-web` (the apex CNAME approach was rejected by GoDaddy with "invalid name for '@'", so the working pattern is `www` as the real Railway custom domain + GoDaddy's domain-forwarding feature for the bare apex -> `www`), `api.conchquest.app` -> `conchquest-api`, and `admin.conchquest.app` -> `conchquest-admin` (both plain subdomain CNAMEs, no forwarding needed). All three have valid, issued SSL certs, confirmed live via `curl`. `mobile/src/lib/supabase.ts`'s `WEB_APP_URL` and `admin/src/lib/api.ts`'s default `API_BASE_URL` both updated to the new custom domains.
- [x] #82 Deploy the admin console (`admin/`) to Railway -- new `conchquest-admin` service, Root Directory `admin`, `serve`-based start script + `railway.json` (same pattern as api/web). Hit two real deploy bugs along the way: (1) `admin/package.json` had no `engines.node` field, so Railway's Nixpacks defaulted to Node 18 and crashed building Vite 8/rolldown (`node:util does not provide an export named 'styleText'`) -- fixed by pinning `engines.node: ">=22.12.0"`, same as web/'s create-astro scaffold already had; (2) `CORS_ALLOWED_ORIGINS` on the API needed `https://admin.conchquest.app` added (it only had local-dev origins by default). Verified fully end-to-end on the real deployed domain: login page renders, pre-login 401/refresh flow fires correctly, CORS allows the credentialed cross-origin request, and after a real login the dashboard loads live data with `document.cookie` still empty (httpOnly confirmed in production, not just locally).
- [x] #87 Fix `web/` serving every route as the home page in production -- `serve`'s `-s` (single-page-app) flag rewrites all paths to the root `index.html`, correct for a client-routed SPA (admin/) but wrong for web/'s real static multi-page site, where `/download`, `/privacy`, `/reset-password` etc. are each a genuine separate `index.html` on disk. This silently broke every route except home in production (caught by user testing, not by initial verification -- the earlier "confirmed working" check only tested HTTP status codes, not actual page content, and would have passed even with this bug present). Verified fix locally and on the live domain: each route now returns its own distinct content.
- [x] #88 Specialize `web/`'s contact-email touchpoints -- the generic `hello@conchquest.app` used across the site is now `waitlist@conchquest.app` (home page waitlist form), `info@conchquest.app` (footer Contact link), `terms@conchquest.app` (Terms contact line), and `privacy@conchquest.app` (Privacy contact line, already correct). Verified in the built HTML and live on the deployed domain.
- [x] #89 Redesign `web/`'s nav/footer for consistency and add Shell Shop + About pages -- new shared `Nav.astro` (logo links home, "Shell Shop"/"About" links upper right, used identically on every page) and `Footer.astro` (copyright + Terms/Privacy/Contact, now the same on every page including `/reset-password`) replace the old per-page nav variants (home's "Get notified" CTA, other pages' "← Back to home" link, `reset-password`'s bare brand-only nav) and footer variants (`site-footer` vs `site-footer-simple`). New `/shop` page ("Shell Shop" -- Coming soon) and `/about` page (full narrative on the Shelling Score, Shelling Strategy AI summary, multi-day forecast, community find map, shell library, and saved-beach alerts). Also removed the "Draft -- not yet reviewed by an attorney" banners from Terms and Privacy now that real drafted content is live (final attorney review is still tracked separately as #75). Verified locally and on the live domain.
- [x] #90 Set up real inboxes for `conchquest.app` via Google Workspace -- domain verified with a Google-provided TXT record, MX records pointed at Google, `info@conchquest.app` created as the one real mailbox with `privacy@`, `terms@`, `waitlist@`, and `safety@` (added for the Community Guidelines contact line, #75) added as aliases on that same user (all land in the one inbox, no code/DNS trickery needed beyond the aliases themselves). Also merged SPF into a single valid TXT record (`v=spf1 include:_spf.google.com include:dc-aa8e722993._spfm.conchquest.app ~all`) combining Google's mail-sending SPF with GoDaddy's pre-existing domain-forwarding SPF, since two separate SPF TXT records on one domain is invalid and hurts deliverability. Verified via `dig` against Google's/Cloudflare's public resolvers and directly against GoDaddy's authoritative nameservers.
- [x] #92 Set up branded Supabase Auth emails via Resend -- verified `conchquest.app` with Resend (DKIM TXT + MX/SPF on a `send.` subdomain, no conflict with the root domain's existing SPF), generated an API key, and wired `smtp.resend.com`/port 587/user `resend` into Supabase's Auth > Emails > SMTP Settings with sender `no-reply@conchquest.app`; that unlocked template editing (previously gated behind custom SMTP). Drafted branded HTML (matching the site's teal/cream palette, system-font fallbacks since email clients don't support the site's Fraunces font) for the two templates the app actually uses: Confirm signup and Reset Password. Verified end-to-end: a real reset email arrived from `no-reply@conchquest.app` with the branded template rendering correctly. Also enabled Supabase's built-in "Password changed" security notification (off by default) and gave it the same branded treatment, linking to `{{ .SiteURL }}/reset-password` for the "wasn't you?" case.
- [x] #91 Add an in-app "Change password" option to Settings for already-logged-in users -- new "Change password" row in Profile's Settings sheet, right after "Edit profile", opening a sheet with Current/New/Confirm password fields. `AuthProvider.changePassword()` re-authenticates with the current password first (via `signInWithPassword`) before calling `supabase.auth.updateUser({password})` -- the standard "Change Password" pattern (proves the caller knows the old password) as opposed to "Forgot Password" (which doesn't need this since a fresh recovery link already proves inbox access instead). Verified the underlying re-auth-then-update sequence end-to-end via a disposable account (correct current password succeeds, wrong one is rejected, new password works afterward, old one no longer does); the mobile UI itself needs on-device verification via the next TestFlight build since this session's web preview environment can't exercise React Navigation screens.
- [x] #79 Stop keeping the Railway Postgres public URL sitting in local `.env` files -- confirmed the deployed `conchquest-api` service already used Postgres's private `postgis.railway.internal` URL (not the public proxy), so it was safe to remove the public TCP proxy (`railway tcp-proxy delete`) without affecting production; verified via a live `curl` against `api.conchquest.app` afterward. Along the way found and fixed a real side effect: the Postgres service's own `DATABASE_URL` variable referenced the now-deleted `RAILWAY_TCP_PROXY_DOMAIN`/`RAILWAY_TCP_PROXY_PORT` variables and had gone blank, which broke `railway connect`'s sanity check -- repointed it to the private domain, matching the existing `DATABASE_PRIVATE_URL`. Local `api/.env` and dev/debugging now go through `railway connect conchquest-postgres --tunnel-only --ssh -P 5433` (port 5433, not 5432, since a local Postgres install may already be on 5432); documented end-to-end, including for other contributors needing TablePlus access, in `docs/TABLEPLUS_DATABASE_ACCESS.md`. Verified with a real `psql` query through the tunnel.
